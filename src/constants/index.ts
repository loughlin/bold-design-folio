// Session storage keys
export const SESSION_TOKEN_KEY = "portfolio_session_token";

// Navigation section IDs
export const NAV_SECTIONS = {
  HERO: "hero",
  WORK: "work",
  APPROACH: "approach",
  EXPERIENCE: "experience",
  ABOUT: "about",
  CONTACT: "contact",
} as const;

// Animation delays
export const ANIMATION_DELAYS = {
  STAGGER: 0.1,
  SHORT: 0.1,
  MEDIUM: 0.2,
  LONG: 0.3,
} as const;
